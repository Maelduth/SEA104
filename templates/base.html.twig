<!DOCTYPE html>
<html>
 <head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="stylesheet" href="https://bootswatch.com/5/flatly/bootstrap.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@400;700&display=swap">
    {% block stylesheets %}
    <style>
        body {
            background: linear-gradient(to right, rgb(230, 232, 235), #6610f2);
            color: white;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
        .container {
            padding: 20px;
            border-radius: 10px;
            flex: 1;
        }
        .header-title {
            color: pink;
            font-family: 'Dancing Script', cursive;
            text-decoration: underline;
            font-size: 48px;
        }
        .info-list {
            color: black;
            font-size: 28px;
            font-family: 'Dancing Script', cursive;
            line-height: 2;
            margin-top: 20px;
        }
        .info-list .highlight {
            text-decoration: underline;
        }
        .footer {
            background-color: #f8f9fa;
            padding: 10px 0;
            position: fixed; /* Changez absolute en fixed */
            bottom: 0;
            width: 100%;
        }
        .photo img {
            max-width: 150px;
            border-radius: 50%;
        }
        .more-info-title {
            font-family: 'Dancing Script', cursive;
            font-size: 36px;
            color: pink;
            text-decoration: underline;
        }

        .header-title {
            font-size: 72px;
            color: #ffffff;
            text-transform: uppercase;
            margin-bottom: 30px;
            text-decoration: none; /* Supprime la soulignure */
        }
        .row {
            height: calc(100vh - 100px); /* Hauteur totale de la page moins le titre */
        }
        .personal-info {
            color: #333;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center; /* Centrer les éléments */
            border-radius: 10px;
        }
        .personal-info img {
            max-width: 150px;
            border-radius: 50%;
            margin-bottom: 20px;
        }
        .personal-info ul {
            list-style: none;
            padding: 0;
            text-align: center; /* Centrer le texte */
        }
        .personal-info ul li {
            margin-bottom: 10px;
            font-size: 18px;
        }
        .right-column {
            display: flex;
            flex-direction: column;
            justify-content: flex-start; /* Aligner les boutons et contenus en haut */
            gap: 20px; /* Espacement entre les boutons */
            height: 100%;
        }
        .button-group {
            display: flex;
            flex-direction: column;
            gap: 20px; /* Augmente l'espacement entre les boutons */
        }
        .button-group button {
            width: 100%;
            font-size: 18px;
        }
        .cv-section {
            display: none; /* Masqué par défaut */
        }
        .cv-section ul {
            list-style: none;
            padding: 0;
            margin-top: 10px; /* Ajout d'espace entre le bouton et le contenu */
        }
        .cv-section ul li {
            margin-bottom: 10px;
            font-size: 16px;
        }
        .header-text {
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 20px;
            text-align: center;
            background: none;
        }
        .footer {
            background-color: #f8f9fa;
            padding: 10px 0;
            text-align: center;
            color: black;
        }
        .flip-card {
            background-color: transparent;
            width: 100%;
            height: 300px; /* Réduit la hauteur des cartes */
            perspective: 1000px;
            margin-bottom: 0px;
        }
        .flip-card-inner {
            position: relative;
            width: 100%;
            height: 100%;
            text-align: center;
            transition: transform 0.6s;
            transform-style: preserve-3d;
        }
        .flip-card:hover .flip-card-inner {
            transform: rotateY(180deg);
        }
        .flip-card-front, .flip-card-back {
            position: absolute;
            width: 100%;
            height: 100%;
            backface-visibility: hidden;
            border-radius: 10px;
            overflow: hidden;
        }
        .flip-card-front {
            background-color: rgba(255, 105, 180, 0.4);
            color: black;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
        }
        .flip-card-back {
            background-color: rgb(9, 168, 134);
            color: white;
            transform: rotateY(180deg);
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            padding: 20px;
        }
        .gallery {
            margin-top: 0px;
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
        }
        .gallery img {
            width: 300px; /* Augmente la largeur des images */
            height: 200px; /* Augmente la hauteur des images */
            object-fit: cover;
            border-radius: 10px;
        }
        .header-container {
            text-align: center;
            margin-top: 20px;
        }
        .portfolio-container {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
            margin-top: 30px;
            flex-wrap: wrap;
        }
        .flip-card {
            background-color: transparent;
            width: 200px;
            height: 300px;
            perspective: 1000px;
            position: relative;
            margin: 10px; /* Ajoute un espace entre les cartes */
        }
        .flip-card-inner {
            position: relative;
            width: 100%;
            height: 100%;
            text-align: center;
            transition: transform 0.6s;
            transform-style: preserve-3d;
        }
        .flip-card.flipped .flip-card-inner {
            transform: rotateY(180deg);
        }
        .flip-card-front, .flip-card-back {
            position: absolute;
            width: 100%;
            height: 100%;
            backface-visibility: hidden;
            border-radius: 10px;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
        }
        .flip-card-front {
            background-color: #3498db;
            color: white;
            font-size: 24px;
            font-weight: bold;
        }
        .flip-card-back {
            background-color: #2ecc71;
            color: white;
            transform: rotateY(180deg);
            padding: 20px;
        }
        .flip-card-back button {
            margin-top: 10px;
            padding: 10px;
            background: white;
            color: #2ecc71;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 14px;
        }
        .flip-card-back button:hover {
            background: #27ae60;
            color: white;
        }
        .iframe-container {
            display: none;
            width: 60%;
            height: 80vh;
            margin: 20px auto;
            border: 2px solid black;
            background: transparent;
        }
        .iframe-container iframe {
            width: 100%;
            height: 100%;
            border: none;
            background: transparent;
        }
    </style>
    {% endblock %}
 </head>
 <body>
 <nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link {% if app.request.get('_route') == 'app_home' %}active{% endif %}" href="{{ path('app_home') }}" data-fr="Home" data-en="Home">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {% if app.request.get('_route') == 'app_portfolio' %}active{% endif %}" href="{{ path('app_portfolio') }}" data-fr="Portfolio" data-en="Portfolio">Portfolio</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {% if app.request.get('_route') == 'app_cv' %}active{% endif %}" href="{{ path('app_cv') }}" data-fr="CV" data-en="CV">CV</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {% if app.request.get('_route') == 'app_passion' %}active{% endif %}" href="{{ path('app_passion') }}" data-fr="Passions" data-en="Passions">Passions</a>
        </li>
      </ul>
    </div>
  </div>
 </nav>
 {% block body %}{% endblock %}
 {% block javascripts %}
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const translateButton = document.getElementById('translate-button');
            let isTranslated = false;

            if (translateButton) {
                translateButton.addEventListener('click', function () {
                    const elementsToTranslate = document.querySelectorAll('[data-fr], [data-en]');
                    elementsToTranslate.forEach(element => {
                        if (isTranslated) {
                            element.innerHTML = element.getAttribute('data-fr');
                        } else {
                            element.innerHTML = element.getAttribute('data-en');
                        }
                    });
                    isTranslated = !isTranslated;
                });
            }
        });
    </script>
 {% endblock %}
 </body>
</html>